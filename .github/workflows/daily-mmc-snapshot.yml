name: Daily Mini Makers Club snapshot

on:
  schedule:
    - cron: '7 9-12 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Fetch Flodesk snapshot
        env:
          FLODESK_API_KEY: ${{ secrets.FLODESK_API_KEY }}
          FORCE_RUN: ${{ github.event_name == 'workflow_dispatch' && 'true' || 'false' }}
        run: node scripts/fetch-flodesk-mmc-snapshot.mjs

      - name: Commit updated history
        run: |
          git config user.name "mmc-dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          git add mmc/data/history.json
          git diff --cached --quiet || git commit -m "Daily Mini Makers Club snapshot: $(date -u +%F)"
          git push
